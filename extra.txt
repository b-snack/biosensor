    # Uncomment below to run additional examples:
    
    # example_quick_start()
    # example_parameter_tuning()
    # example_custom_events()
    
    # Uncomment to test advanced features:
    
    # model_string = create_sclerostin_biosensor_model()
    # sim = BiosensorSimulator(model_string)
    # optimized_params = optimize_biosensor(sim, target_snr=50.0)
    # variant_comparison = test_circuit_variants(sim)
    # export_sbml(sim, 'my_biosensor.xml')